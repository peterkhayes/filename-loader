{
  "name": "filename-loader",
  "version": "1.0.0",
  "description": "Webpack loader that returns the name of the file as a string.  Useful for testing image paths",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Peter Hayes",
  "license": "ISC",
  "keywords": ["webpack", "filename", "name", "image", "path", "loader"]
}
